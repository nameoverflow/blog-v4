import React from 'react'
import { Link } from 'react-router'

import Default from './Default'
import Mobile from './Mobile'

if (typeof window !== 'undefined') {
    import style from 'HeaderNav.sass'
}
